server {
  # Écoute les requêtes sur le port 80
  listen 80;

  # Redirige les requêtes du site vers le service front-end
  location / {
    proxy_pass http://front-end:9000;
  }

  # Redirige les requêtes API vers le service back-end
  location /api/ {
    proxy_pass http://back-end:5252;
  }
}